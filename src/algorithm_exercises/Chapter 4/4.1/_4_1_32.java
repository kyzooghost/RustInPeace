// Parallel edge detection. Devise a linear-time algorithm to count the parallel edges in a graph.

// Just iterate through the adjacency matrix, and if there are duplicate entries you have a parallel edge.

public class _4_1_32 {
    
}
