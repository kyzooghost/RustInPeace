#![allow(non_snake_case)]
#![allow(non_camel_case_types)]

// p522

// 3.5.22 - Fully Indexed CSV

// Implement an ST client FullLookupCSV that builds an ar- ray of ST objects (one for each field), with a test client that allows the user to specify the key and value fields in each query.

// 


fn main() {
}